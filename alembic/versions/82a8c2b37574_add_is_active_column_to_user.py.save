revision = '5da85f6151a0'
down_revision = 'ac5360dd76bc'  # replace with last migration applied in your DB
branch_labels = None
depends_on = None
from alembic import opimport sqlalchemy as sa


revision = '82a8c2b37574'
down_revision = '5da85f6151a0' 
branch_labels = None
depends_on = None

def upgrade() -> None:
    
    op.add_column(
        'users',
        sa.Column('is_subscribed', sa.Boolean(), server_default=sa.text('true'), nullable=False)
    )

def downgrade() -> None:
    op.drop_column('users', 'is_subscribed')

